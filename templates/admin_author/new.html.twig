{% extends 'base-backend.html.twig' %}

{% block title %}Nouvel(le) auteur(e){% endblock %}

{% block body %}
    <a href="{{ path('app_admin_author_index') }}"class="btn btn-dark btn-sm">Retour à la liste</a>

    <div class="d-flex justify-content-between align-items-center">
        <h1 class="mt-3">Nouvel(le) auteur(e)</h1>
    </div>

    {{ include('admin_author/_form.html.twig', {'button_label': 'Enregistrer'}) }}

{% endblock %}
