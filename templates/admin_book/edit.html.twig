{% extends 'base-backend.html.twig' %}

{% block title %}Edition du livre{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_book_index') }}" class="btn btn-dark btn-sm">Retour à la liste</a>
    <div class="d-flex justify-content-between align-items-center">
        <h1 class="mt-3">Edition du livre</h1>
        {{ include('admin_book/_delete_form.html.twig') }}
    </div>

    {{ include('admin_book/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}